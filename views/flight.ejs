<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@3.4.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
    <nav>
        <div class="nav__logo">Boobs</div>
        <ul class="nav__links">
          <li class="link"><a href="#">Home</a></li>
          <li class="link"><a href="#">About</a></li>
          <li class="link"><a href="#">Offers</a></li>
          <li class="link"><a href="#">Seats</a></li>
          <li class="link"><a href="#">Destinations</a></li>
        </ul>
        <button class="btn">Contact</button>
      </nav>
      <% if (trips && trips.length > 0) { %>
        <ul>
            <% trips.forEach((flight) => { %>
                <li>From: <%= flight.from %></li>
                <li>To: <%= flight.to %></li>
                <li>Flight Name: <%= flight.flightName %></li>
                <li>Capacity : <%= flight.capacity.economy %></li>
                <li>Price: <%= flight.price.economy %></li>
                <li>Departure: <%= flight.departureTime %></li>
                <li>Arrival: <%= flight.arrivalTime %></li>

            <% }); %>
        </ul>
    <% } else { %>
        <p>No flights found for the specified criteria.</p>
    <% } %>
</body>
</html>