<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@3.4.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
    <nav>
        <div class="nav__logo">Boobs</div>
        <ul class="nav__links">
          <li class="link"><a href="/">Home</a></li>
          <li class="link"><a href="/">About</a></li>
          <li class="link"><a href="/">Offers</a></li>
          <li class="link"><a href="/">Seats</a></li>
          <li class="link"><a href="/">Destinations</a></li>
        </ul>
        <button class="btn">Contact</button>
    </nav>
    <% if (tickets.length > 0) { %>
        <div class="tickets">
            <h3>Your Tickets</h3>
            <ul>
                <% tickets.forEach(ticket => { %>
                    <li>
                        <div class="ticket">
                            <div class="ticket__header">
                                <h4><%= ticket.trip.flightName %></h4>
                                <p><%= ticket.trip.flight %></p>
                            </div>
                            <div class="ticket__body">
                                <p><%= ticket.trip.from %> to <%= ticket.to %></p>
                                <p><%= ticket.trip.departureTime %> - <%= ticket.arrivalTime %></p>
                            </div>
                        </div>
                    </li>
                <% }) %>
            </ul>
        </div>
    <% } else { %>
        <div class="alert alert-danger" role="alert">
            <strong>Failed!</strong> You have not traveled anywhere yet .
        </div>
    <% } %>
</body>